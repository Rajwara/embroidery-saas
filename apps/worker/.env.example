REDIS_URL=redis://localhost:6379/0
RESEND_API_KEY=
# Base URL of apps/api -- the worker has no DB access of its own (see
# tasks.py's docstring); it fetches report data over HTTP instead.
API_BASE_URL=http://localhost:8000
# Must match apps/api's INTERNAL_API_SECRET exactly.
INTERNAL_API_SECRET=change-me-in-every-environment
